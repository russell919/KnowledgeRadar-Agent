# 2026-05-06 — Step 2 (P1) 完成：GraphRAG + 版本历史 + 去重

## 工作内容

### GraphRAG 完整实现（P1）
- [x] 创建 `graphrag.js` — 轻量知识图谱增强引擎
  - `traverse()`: BFS 关系加权遍历，按实体类型权重排序
  - `buildContext()`: 为实体集合聚合关联知识（决策/任务/风险/人员/项目）
  - `analyzeImpact()`: 文档变更影响分析
  - `buildProjectOverview()`: 项目全景图（新人入职脉络生成）
- [x] 集成到 `executors.js`（4个场景全部支持 GraphRAG 上下文增强）
  - MeetingBriefing: GraphRAG 补充参会人关联决策/风险/未闭环事项
  - WeeklyDigest: GraphRAG 活跃实体上下文
  - DocChange: GraphRAG 影响分析（受影响的项目/人员/待办）
  - Onboarding: GraphRAG 项目脉络图（人数/决策/文档/任务/风险统计）
- [x] `POST /v1/knowledge/graphrag` 端点（4种 action）
- [x] TypeScript `get_knowledge_graph` 工具扩展（支持 `mode: "graphrag"`）

### 文档版本历史 (P1)
- [x] `document_versions` 表（版本号自动递增，内容哈希）
- [x] `saveDocumentVersion()` / `getDocumentVersions()` / `getDocumentVersion()` 函数
- [x] `POST /v1/documents/version` — 保存版本
- [x] `POST /v1/documents/versions` — 获取版本历史

### 去重 (P1)
- [x] `POST /v1/knowledge/dedup` — 字符级 Jaccard 相似度去重
- [x] 支持自定义 threshold（默认0.5，生产建议0.8+）

### 后端
- [x] `server.js` 新增6个端点
- [x] TypeScript 插件重建通过
- [x] 服务运行验证通过
- [x] E2E 场景测试通过

## 验证结果

### GraphRAG 上下文聚合
```
请求: POST /v1/knowledge/graphrag (action=context, entities=["微服务架构","张三"])
结果: Decisions:2 Tasks:3 Risks:3 People:3
```

### 项目脉络图
```
请求: POST /v1/knowledge/graphrag (action=project-overview, project="微服务架构项目")
结果: Nodes:6 People:1
```

### 文档版本
```
version 1: "微服务架构设计文档 v2"  (change: "更新了认证模块设计")
version 2: "v2" (content_hash: e7c)
```

### 去重 (threshold=0.8)
```
Found 12 duplicates (含 "采用微服务架构方案" 0.94, "数据库迁移风险" 1.0, "认证模块开发" 0.88)
```

### E2E 会前简报
```
LLM 生成摘要成功: "上次会议已决定采用微服务架构方案，目前需跟进认证模块开发等行动项"
含 Hybrid Search + GraphRAG 增强上下文
```

## ✅ Step 3 (P2) 完成 — FAQ + 语义切分 + Text-to-SQL + Onboarding 推送

### 1. FAQ 自动沉淀 ✅
- [x] faq-miner.js — 高频问题聚类 + FAQ 候选生成
- [x] 支持 LLM 生成答案和规则降级
- [x] 聚类算法（字符级 Jaccard）+ 贪心聚类
- [x] faq_candidates 表 + review/publish/reject 流程
- [x] 端点：POST /v1/faq/mine, /v1/faq/get, /v1/faq/review
- [x] 验证：成功从消息中挖掘 2 个 FAQ 候选

### 2. 文档语义切分 ✅
- [x] chunker.js — 语义段落分块 + 独立索引
- [x] document_chunks 表
- [x] 按段落(

)分割 + 句号降级
- [x] 目标块大小 500字，重叠 50 字
- [x] Chunk 自动索引到 Hybrid Search
- [x] 端点：POST /v1/documents/chunk, /v1/documents/chunks

### 3. Text-to-SQL ✅
- [x] text-to-sql.js — 自然语言转数据库查询
- [x] LLM 生成 SQL + 安全校验（仅 SELECT）
- [x] 规则降级（无 LLM 时 10+ 种常用查询模式）
- [x] 12 张表描述 + 可查询白名单
- [x] 结果自然语言摘要
- [x] 端点：POST /v1/query/sql
- [x] 验证："最近的决策"→ SQL 查询 5 条决策 ✓

### 4. Onboarding 每日推送 ✅
- [x] onboarding-push.js — Day 1~7 线性推送
- [x] Day 1: 项目概览 | Day 2: 关键决策 | Day 3: 待办事项
- [x] Day 4: 风险 | Day 5: 关键联系人 | Day 6: FAQ | Day 7: 总结
- [x] 新人进度自动推进（advance_all）
- [x] 端点：POST /v1/onboarding/push
- [x] 验证：Day 1~2 推送内容正确生成

### 后端状态
- 插件版本：v2.2.0
- 总计 22 个 API 端点
- 数据库表：15 张
- 消息数：7 | FAQ 候选：2 | Knowledge：21 | Entities：44
- TypeScript 编译通过 ✅
